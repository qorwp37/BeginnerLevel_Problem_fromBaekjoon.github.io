//c언어
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main()
{
	int size;
	scanf("%d", &size);
	char input[100] = { 0 };
	scanf("%s", &input);

	int sum = 0;
	for (int i = 0; i < size; i++)
	{
		sum += input[i] - '0';
	}
	printf("%d", sum);
	return 0;
}
